package com.skysense.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Space dark palette ────────────────────────────────────────────────────────
val SpaceBlack        = Color(0xFF08090D)
val SpaceDeep         = Color(0xFF0D1117)
val SpaceDark         = Color(0xFF111827)
val SpaceCard         = Color(0xFF1A2233)
val SpaceCardElevated = Color(0xFF1F2B40)
val SpaceDivider      = Color(0xFF263247)

// ── Primary accent ────────────────────────────────────────────────────────────
val CosmicBlue        = Color(0xFF4A9EFF)
val CosmicBlueDark    = Color(0xFF1A6FCC)
val CosmicBlueLight   = Color(0xFF82BFFF)

// ── Status colors ─────────────────────────────────────────────────────────────
val SignalExcellent   = Color(0xFF2DD4A0)   // teal-green
val SignalGood        = Color(0xFF7AC94F)   // green
val SignalFair        = Color(0xFFF5A623)   // amber
val SignalPoor        = Color(0xFFFF5C5C)   // red

// ── Constellation accent colors ───────────────────────────────────────────────
val GpsColor          = Color(0xFF4A9EFF)   // GPS → blue
val GalileoColor      = Color(0xFFFF8C42)   // Galileo → orange
val GlonassColor      = Color(0xFFFF5C5C)   // GLONASS → red
val BeiDouColor       = Color(0xFF2DD4A0)   // BeiDou → teal
val QzssColor         = Color(0xFFBF7FFF)   // QZSS → purple
val IrnssColor        = Color(0xFFE91E63)   // IRNSS → pink
val SbasColor         = Color(0xFF7AC94F)   // SBAS → green
val UnknownSatColor   = Color(0xFF6B7A99)   // Unknown → muted

// ── Text ──────────────────────────────────────────────────────────────────────
val StarWhite         = Color(0xFFF0F4FF)
val MoonGrey          = Color(0xFFB0BDD8)
val DimGrey           = Color(0xFF6B7A99)

// ── Surface ───────────────────────────────────────────────────────────────────
val OnSurface         = StarWhite
val SurfaceVariant    = SpaceCardElevated
